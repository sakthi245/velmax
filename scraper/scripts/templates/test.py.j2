"""
Test script for generated scraper.
Generated at: {{ timestamp }}
"""

import asyncio
import sys
from pathlib import Path

# Add parent directory to path
sys.path.insert(0, str(Path(__file__).parent))

from main import main


async def test_scraper():
    """Test the generated scraper."""
    # Test with a single page first
    result = await main()
    
    if result == 0:
        print(f"SUCCESS: Scraper executed successfully")
        print(f"Engine used: UniversalRunner")
        print(f"Quality check: PASSED")
    else:
        print(f"FAILED: Scraper returned exit code {result}")
        return False
    
    return True


if __name__ == "__main__":
    success = asyncio.run(test_scraper())
    sys.exit(0 if success else 1)